# infrastructure

## location
compose = /projects
nginx = /etc/nginx
ddclient = /etc


## commands

```
docker compose down
```
```
docker compose pull
```
```
docker compose up -d
```

```
certbot --nginx-server-root=/etc/nginx --nginx-ctl=/usr/sbin/nginx
```


